// 电影播放器(影视播放器).
class Player {
    // 播放历史.
    playerHistory = [];
    // 操作历史.
    handleHistory = [];

    play({MovieType: movieType}) {
        switch (movieType) {
        }
    }

    // 记录播放历史.
    queue() {
    }

    // 跳转指定步骤视图.
    jumpStepView() {
    }
}